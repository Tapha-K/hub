# Design Direction

## 역할

이 문서는 나의 책장 서비스의 시각적 방향을 정리한다. 현재 우선순위는 디자인 연출이 아니라 `책 등록 → 세션 기록 저장 → 다음 페이지부터 재개`가 실제 서버 데이터로 동작하는 것이다. 시각 요소는 이 흐름을 더 쉽게 읽게 만드는 범위에서만 적용한다.

제품 정책과 범위는 `client/docs/product-prd.md`를 기준으로 한다.

## 핵심 인상

`읽은 페이지와 생각이 한 권의 독서 노트에 쌓이고, 다음에 다시 그 책을 꺼내는 개인 서재`

- 책이 주인공이고 타이머는 보조 도구다.
- 책장은 꾸미기 게임이 아니라, 어떤 책을 다시 열지 알려 주는 탐색 구조다.
- 세션 기록은 실제 책의 페이지를 복제하지 않고, 읽은 범위와 짧은 감상이 담긴 기록 페이지 한 장이다.
- 완독 서평 페이지는 다음 단계의 목적지다. P0에서 잠금·해제 연출을 과하게 구현하지 않는다.

## P0 시각 우선순위

1. 재방문 시 `최근 이어 읽기`와 다음 시작 페이지가 가장 먼저 읽힌다.
2. 책장에서는 책 제목, 읽는 중 상태, 마지막 페이지 또는 다음 시작 페이지를 찾을 수 있다.
3. 책 상세에서는 마지막 책갈피와 `이어 읽기` CTA가 세션 기록 목록보다 먼저 보인다.
4. 기록 저장 뒤에는 새 기록 페이지와 갱신된 다음 시작 페이지가 즉시 보인다.
5. 빈 책장은 부족함이 아니라 `첫 책이 들어올 자리`로 보인다.
6. P1의 읽기 기록 달력은 최근 독서 날짜를 조용히 보여 주되, 스트릭·결석·점수처럼 보이지 않는다.

## 화면 방향

### 내 책장

- 상단에는 최근 읽은 한 권을 강조한 재개 카드를 둔다.
- 그 아래에 `읽는 중` 책 목록을 최신 기록 순서로 둔다.
- P0의 책등은 일관된 카드 또는 세로형 목록으로 충분하다. 물리적 선반, 3D 책, 드래그 배치는 구현하지 않는다.
- 책 제목이 잘리지 않게 하고, 작은 텍스트로 마지막 기록 날짜와 다음 시작 페이지를 보조한다.

### 책 상세

- 책 표지나 제목은 해당 책을 식별하는 역할만 한다. 외부 표지 수집은 하지 않는다.
- `지난번 58쪽까지 읽었어요`와 `59쪽부터 이어 읽기`를 상단 행동으로 둔다.
- 세션 기록은 오래된 순서로 쌓인 노트 페이지처럼 보이되, 페이지 범위와 날짜가 가장 잘 읽혀야 한다.
- 감상이 없는 기록은 비어 보이지 않도록 페이지 범위와 날짜를 충분한 정보로 표시한다.
- 하단의 마지막 서평 미리보기는 완독을 유도하는 짧은 안내만 제공한다.
- P2의 생각 키워드는 감상 아래의 작은 칩으로 표시하고, 키워드 모아보기에서는 책 제목과 페이지 범위를 잃지 않게 한다.

### 입력 dialog

- 책 등록은 제목을 가장 먼저 두고, 저자와 시작 페이지는 보조 입력으로 둔다.
- 세션 기록은 자동으로 보여 주는 시작 위치, 사용자가 입력하는 끝난 페이지, 선택적 한 줄 감상 순서로 둔다.
- 시작 위치 입력은 다시 읽기·건너뛰기 같은 예외에서만 `시작 위치 직접 바꾸기`로 펼친다.
- 감상 입력에는 `한 줄이면 충분해요. 비워 두어도 괜찮아요.`를 명시한다.
- 오류는 필드 바로 아래에 보여 주고, 저장 실패 후 draft를 지우지 않는다.

## 색·타이포그래피·표면

- 차분한 종이, 잉크, 나무 계열의 제한된 색을 사용한다.
- 제목에는 읽기 좋은 세리프를 선택할 수 있지만, 버튼·입력·수치·페이지 범위는 명확한 산세리프를 사용한다.
- 과한 보라색 그라디언트, glow, 장식용 blob, 게임형 뱃지를 사용하지 않는다.
- 카드와 경계는 책장 구조를 읽게 하는 수준으로만 사용한다. 모든 정보를 둥근 카드로 겹치지 않는다.
- 의미 있는 아이콘 버튼에는 항상 label 또는 `aria-label`을 제공한다.

## P0에서 하지 않을 시각 작업

- 책이 꺼내져 펼쳐지는 복잡한 애니메이션
- 실제 책 표지 이미지와 외부 도서 검색
- 책등 디자인 해금, 서가 테마, 컬렉션 편집
- P1 읽기 기록 달력보다 복잡한 독서 발자취 지도
- P2 생각 키워드 모아보기
- 친구의 서재, 공개 서평, 반응 UI

이 요소들은 개인 책장의 재개 루프가 검증된 뒤 추가한다. 이후 커스터마이징은 읽기 여정에 따라 사용자가 선택하는 보상이어야 하며, 점수나 타인 비교를 강화하는 장치가 되어서는 안 된다.

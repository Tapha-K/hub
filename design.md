# 나의 책장 디자인 가이드

> 이 문서는 **나의 책장** PC 웹 MVP의 시각·상호작용 기준이다. 제품 정책과 데이터 규칙은 `client/docs/product-prd.md`, 클릭 결과는 `client/docs/click-unit-ui-spec.md`를 따른다. 이 문서는 그 기능을 **실제로 책을 꺼내 테이블에서 펼치는 작은 독서방**으로 보이게 만드는 방법에 집중한다.

## 1. 디자인 목표

서비스의 핵심은 서재를 꾸미는 일이 아니라 마지막 책갈피에서 같은 책을 다시 여는 일이다.

```text
방에 들어온다
→ 책장에서 읽던 책을 고른다
→ 책이 선반에서 빠져나와 테이블에 놓인다
→ 마지막 책갈피부터 읽고 기록한다
→ 책이 다시 자기 자리로 돌아간다
```

첫 화면은 카드 목록 위에 책장 그림을 얹은 대시보드가 아니다. 헤더 아래 전체가 하나의 방이며, 중앙의 책장과 앞쪽 테이블이 탐색·재개·기록의 무대가 된다.

### 한 줄 인상

`조금 삐뚤고 따뜻한 내 방에서, 읽던 책을 직접 꺼내 다시 펼치는 개인 서재`

### 구현 경계

- 기존 `@react-three/fiber`, `@react-three/drei`, `@react-spring/three` 구현을 확장한다. 새 3D·모션 의존성은 추가하지 않는다.
- 방은 Three.js 기본 geometry, 단순 material, 기존 `RoundedBox`, `ContactShadows`로 만든다.
- 제목·페이지·버튼·form·dialog는 DOM으로 둔다. Canvas 안에 핵심 정보를 그리지 않는다.
- 외부 표지 URL, Joguman 캐릭터·소품 이미지, 사진 배경을 복제하거나 포함하지 않는다.
- 물리 엔진, 자유 카메라, 드래그 배치, 사실적인 페이지 시뮬레이션은 만들지 않는다.

## 2. 시각 참조 해석

참조: [JOGUMAN](https://www.joguman.com/)

### 가져올 것

- 화면 전체를 하나의 생활 공간으로 쓰고, 넓은 빈 벽과 아래쪽 가구로 장면을 만드는 구성
- 책장·램프·액자처럼 화면 속 물건 자체가 목적지이자 조작 대상이 되는 방식
- 반듯한 벡터보다 손으로 그린 듯 조금 흔들리는 검은 윤곽과 단순한 면
- 아이보리 바탕에 옅은 파랑, 나무색, 노랑·초록을 조금만 쓰는 제한된 팔레트
- hover·focus 때 짧은 말풍선이나 물건의 작은 움직임으로 용도를 알려 주는 피드백
- 소품이 많아도 각 물건 사이에 여백이 있어 장면이 답답하지 않은 밀도

### 그대로 가져오지 않을 것

- 캐릭터, 로고, 소품의 구체적인 형태와 원본 에셋
- 콘텐츠와 관계없는 이스터에그, 화면 흔들기, 게임성 상호작용
- 모든 소품을 클릭 가능하게 만드는 방식
- 여러 색이 동시에 번쩍이는 hover, 긴 인트로, 사용을 막는 연출

참조의 핵심은 특정 그림체 복제가 아니라 **인터페이스가 방 안의 물건처럼 존재하는 감각**이다.

## 3. 디자인 원칙

1. **방이 곧 내비게이션이다.** 책은 책장에서 고르고, 선택한 책은 테이블에서 연다.
2. **재개 행동이 가장 먼저 읽힌다.** 최근 책은 책갈피와 짧은 안내로 다른 책보다 먼저 보인다.
3. **물성은 움직임으로 설명한다.** 과한 사실감 대신 `꺼냄 → 옮김 → 놓음 → 펼침`의 순서가 명확해야 한다.
4. **정보는 DOM으로 또렷하게 읽힌다.** 장면은 맥락이고 제목·페이지·오류·CTA는 웹 UI다.
5. **조금 삐뚤되 조작은 정확하다.** 책과 가구는 약간 기울 수 있지만 hit area, focus, form 정렬은 반듯하게 둔다.
6. **한 장면에 한 주 행동만 둔다.** 최근 이어 읽기만 강하게 보이고 책 추가·상세 열기는 보조 행동으로 둔다.

## 4. 색·재질·타입

### 색 토큰

| 역할 | 토큰 | 값 | 용도 |
| --- | --- | --- | --- |
| 방의 빛 | `--canvas` | `#FFFAF0` | 전체 바탕, 빈 벽 |
| 뒷벽 | `--wall` | `#F3E7D3` | 방 내부 배경 |
| 바닥 | `--floor` | `#AFC9DE` | 아래쪽 공간 구분 |
| 종이 | `--paper` | `#FFFDF8` | 안내, dialog, 기록 면 |
| 윤곽 | `--ink` | `#24211F` | 가구 외곽, 제목, 본문 |
| 나무 | `--wood` | `#9B6747` | 책장·테이블 |
| 짙은 나무 | `--wood-dark` | `#654331` | 옆면·아래 면·접촉부 |
| 보조 글자 | `--muted` | `#6F6258` | 날짜·저자·설명 |
| 따뜻한 강조 | `--accent` | `#D96845` | 주 CTA·최근 책갈피 |
| 작은 포인트 | `--yellow` | `#F2C94C` | 램프·작은 표식 |
| focus | `--focus` | `#146B8C` | 키보드 focus ring |
| 오류 | `--danger` | `#B54742` | 검증 오류 |

- 아이보리와 옅은 파랑이 장면 대부분을 차지한다. 강조색은 최근 책갈피와 주 CTA에만 쓴다.
- 본문과 조작 텍스트는 배경 대비 4.5:1 이상을 확보한다. 옅은 나무색 위에는 본문을 직접 놓지 않는다.
- 그라디언트·bloom·유리 효과·금속 광택은 사용하지 않는다.

### 3D 재질

- 가구와 책은 `roughness 0.75–0.95`, `metalness 0–0.04`의 무광 재질을 쓴다.
- 외곽은 검거나 짙은 갈색의 얇은 edge로 잡는다. 새 outline 라이브러리 대신 기존 drei의 `Edges` 또는 한 겹 큰 geometry로 충분하다.
- 손그림 느낌은 매 프레임 흔들지 않는다. 책 높이·폭, 선반 각도, 소품 위치에 1–2°의 고정된 차이만 둔다.
- 조명은 ambient 하나, 따뜻한 directional 하나, 낮은 contact shadow 하나면 충분하다. post-processing은 넣지 않는다.

### 책 팔레트

책 색과 크기는 `book.id`로 결정해 새로고침 뒤에도 같다.

| 톤 | 표지 | 어두운 면 | 표식 |
| --- | --- | --- | --- |
| 벽돌 | `#A94F3B` | `#733124` | `#E6B06D` |
| 먹청 | `#3F5558` | `#26383B` | `#D9B87B` |
| 황토 | `#8B7048` | `#5F472A` | `#F0D7A2` |
| 자주 | `#6F5873` | `#49364D` | `#DEC0D2` |
| 초록 | `#56715C` | `#354B3A` | `#D6C789` |

책은 5–7가지 고정 폭·높이 조합을 반복한다. 완전한 무작위 크기나 매번 달라지는 색은 쓰지 않는다.

### 타입

- 책 제목·페이지 제목: `"Noto Serif KR", Georgia, serif`, 600.
- 본문·입력·버튼: `Geist`, `Pretendard`, system-ui, sans-serif.
- 기본 본문은 16px 이상, 행간은 1.55 이상이다.
- 영어 kicker는 장식이므로 핵심 정보를 대신하지 않는다.

## 5. 책장 화면: 한 화면짜리 독서방

### PC 구성

기준은 1280 × 800 이상이다. 헤더를 제외한 장면은 최소 `calc(100dvh - 64px)`를 채운다.

```text
┌──────────────────────────────────────────────────────────────┐
│ 잇장                                      닉네임의 책장  +책 │
├──────────────────────────────────────────────────────────────┤
│                         빈 벽                                │
│             작은 액자                  따뜻한 조명            │
│                                                              │
│                   ┌──────────────┐                           │
│                   │  책 책 책 책 │  ← 중앙 책장               │
│                   │  책 책       │                           │
│                   └──────────────┘                           │
│            ┌────────────────────────────┐                    │
│            │ 최근 책 안내 / 펼쳐질 자리 │  ← 앞쪽 테이블       │
│            └────────────────────────────┘                    │
└──────────────────────────────────────────────────────────────┘
```

- 책장은 장면 중앙에서 가로 46–56%, 높이 46–54%를 차지한다.
- 테이블은 책장보다 카메라에 가깝고 화면 아래 22–28%를 차지한다. 책이 놓일 중앙 자리는 항상 비워 둔다.
- 가구를 화면 가장자리에 붙이지 않는다. 책장 좌우와 위에는 방으로 읽힐 여백을 남긴다.
- 액자·화분·램프는 각 0–1개만 둔다. P0에서 램프를 클릭 가능한 테마 스위치로 만들지 않는다.
- 카메라는 정면에서 약간 내려다보는 고정 perspective다. 사용자가 orbit·zoom·pan하지 않는다.

### 최근 이어 읽기

- 가장 최근 책은 선반에서 6–10% 앞으로 나와 있고 책갈피가 보여야 한다.
- 책 옆 또는 테이블 위의 작은 종이 안내에 `책 제목`, `지난번 n쪽`, `{nextStartPage}쪽부터 이어 읽기`를 둔다.
- 안내는 독립된 큰 대시보드 카드처럼 장면을 가리지 않는다. 최대 폭 360px, 문장 3줄, CTA 1개로 제한한다.
- 최근 감상은 한 줄이 있을 때만 보인다.
- CTA를 누르면 해당 책이 선반에서 빠져나와 테이블로 이동한다.

### 책 선택

- pointer hover 또는 keyboard focus 시 책이 3–5% 앞으로 나오고 1–2° 바로 선다.
- 책 위에 작은 말풍선으로 `제목 · 다음 n쪽`을 표시한다. 말풍선은 다른 책을 가리지 않으며 2줄을 넘지 않는다.
- 선택하면 책이 실제 slot에서 빠져나온다. 동시에 다른 책을 선택할 수 없도록 해당 전환 동안 hit area를 잠근다.
- Canvas는 `aria-hidden="true"`로 두고 같은 순서의 DOM `<ul><li><button>`이 모든 책을 조작한다.

### 빈 책장

- 방과 빈 책장은 그대로 보여 주고, 선반 한 칸에 책 윤곽만 아주 옅게 남긴다.
- 테이블 위 종이에 `첫 책이 들어올 자리예요.`와 `읽고 있는 책 추가` CTA를 둔다.
- 빈 상태를 경고, 0%, 실패로 표현하지 않는다.

## 6. 책의 물리적 전환

기존 상태 이름을 유지해 새 상태 머신을 만들지 않는다.

```text
idle
→ pulling   선반 앞쪽으로 빠져나옴
→ turning   표지가 보이도록 회전하며 테이블 중앙으로 이동
→ opening   테이블에 닿은 뒤 표지가 열림
→ zooming   열린 책과 DOM 상세 화면으로 시선이 이동
```

### 책 꺼내기

- `pulling`은 180–240ms, 깊이축 이동만 한다.
- `turning`은 280–360ms, 완만한 곡선으로 테이블 중앙까지 이동한다. 공중에서 과하게 튀거나 한 바퀴 돌지 않는다.
- `opening`은 300–420ms로 표지가 열리고, 기록이 있으면 책갈피가 있는 면까지 1–3장만 넘긴다.
- 페이지 수를 실제 기록 수와 일대일로 재현하지 않는다. `기록 없음 0장 / 기록 있음 2장 / 완독 3장` 정도의 짧은 표현이면 충분하다.

### 책 다시 넣기

- 상세에서 책장으로 돌아오면 열린 책이 닫히고 같은 경로를 역순으로 돌아간다.
- 원래 slot은 `book.id` 기준으로 유지한다. 정렬이 바뀐 경우 서버에서 받은 새 순서의 slot으로 돌아간다.
- 복귀 모션 중 페이지 데이터나 저장 결과를 가리지 않는다. 저장 성공 정보가 먼저 갱신된 뒤 이동한다.

### 새 책 꽂기

- 등록 성공 전에는 장면에 책을 추가하지 않는다.
- 성공하면 닫힌 책이 테이블 중앙에 나타나 가장 가까운 빈 slot으로 이동한다.
- 사용자가 직접 끌어 꽂게 하지 않는다. drag-and-drop과 충돌 계산은 P0 범위가 아니다.

### 모션 축소

- `prefers-reduced-motion: reduce`에서는 중간 위치·회전·페이지 넘김을 생략하고 즉시 상세 또는 책장 상태로 전환한다.
- 모션이 없어도 선택한 책 제목과 결과를 `aria-live="polite"`로 알린다.

## 7. 책 상세: 테이블 위 독서 노트

- 상세는 완전히 다른 흰 페이지로 끊기기보다, 방의 배경을 어둡게 남기고 테이블과 펼친 책을 가까이 보여 준다.
- 실제 입력과 기록 목록은 펼친 책 위에 정렬된 DOM 종이 면이다. CanvasTexture 안에 본문을 넣지 않는다.
- 왼쪽 면은 책 제목·저자·지난 책갈피, 오른쪽 면은 `{nextStartPage}쪽부터 이어 읽기`와 최근 기록을 둔다.
- 기록이 길어지면 3D 페이지를 계속 생성하지 않는다. DOM 목록을 스크롤하고, 책은 배경 맥락으로만 남긴다.
- `책장으로`는 왼쪽 위의 명확한 텍스트 버튼이다. 숨은 edge click이나 브라우저 뒤로가기에만 의존하지 않는다.
- 마지막 서평 P1 안내는 오른쪽 면 아래에 작은 문장으로 둔다. 잠긴 자물쇠나 비활성 버튼을 크게 만들지 않는다.

## 8. Dialog와 form

- 책 등록과 세션 기록은 Canvas 안에 구현하지 않고 기존 DOM dialog를 사용한다.
- dialog는 밝은 종이 면, 짙은 1px 테두리, 8–12px 모서리, 낮은 그림자만 쓴다.
- 뒤의 방은 약하게 어두워지지만 책장과 테이블의 위치는 알아볼 수 있어야 한다.
- 책 등록 순서는 `책 이름 검색 → 판본 선택 → 시작 페이지 → 책장에 꽂기`다.
- 기록 순서는 `자동 시작 위치 → 오늘 끝낸 페이지 → 선택 감상 → 기록 페이지 추가`다.
- 저장 중 닫기와 중복 제출을 막고, 실패하면 입력값을 유지한다.
- 오류는 해당 입력 아래에 문장으로 표시하고 첫 오류 필드로 focus를 옮긴다.

## 9. 폭별 규칙

| 화면 폭 | 장면 | 조작 |
| --- | --- | --- |
| 1280px 이상 | 책장·테이블 전체, 좌우 소품 허용 | 말풍선과 최근 안내를 책장 옆에 배치 |
| 1024–1279px | 책장 56–64%, 테이블 폭 축소 | 최근 안내를 테이블 왼쪽에 배치 |
| 768–1023px | 액자·화분 제거, 책장과 테이블만 유지 | 안내를 테이블 위 한 줄 블록으로 축소 |
| 767px 이하 | 3D 방은 배경 맥락으로만 사용 | 화면 아래에 DOM 책 목록과 CTA를 세로 배치 |

- P0는 PC 우선이지만 320 CSS px와 400% 확대에서도 핵심 form·CTA가 잘리면 안 된다.
- 좁은 화면에서 3D 책의 작은 hit area를 억지로 유지하지 않는다. DOM 목록이 주 조작 수단이 된다.
- 가로 스크롤은 모바일 책 목록에서만 허용한다. dialog와 기록 본문은 가로로 스크롤하지 않는다.

## 10. 상태·접근성·성능

### 상태

| 상태 | 장면 반응 |
| --- | --- |
| 불러오는 중 | 방과 책장 silhouette를 먼저, 책 slot만 낮은 대비 skeleton으로 표시 |
| 저장 중 | 현재 책은 테이블에 유지하고 CTA만 비활성화 |
| 저장 성공 | 페이지 정보 갱신 후 책갈피가 새 위치로 짧게 이동 |
| 저장 실패 | 책과 draft를 그대로 두고 dialog 안에 오류·재시도 표시 |
| 책 없음 | 빈 책장과 첫 책 CTA 표시 |
| 책을 찾을 수 없음 | 방 배경 위에 `책장으로 돌아가기` 하나를 표시 |

### 접근성

- DOM 순서는 `h1 → 최근 이어 읽기 → 읽는 중 책 목록 → 책 추가`다. 시각적 좌표와 무관하게 의미 순서를 유지한다.
- 모든 책은 최소 44 × 44px의 DOM hit area와 고유한 접근 가능한 이름을 가진다.
- 이름에는 제목·저자·등록일·다음 시작 페이지를 넣어 중복 책을 구별한다.
- hover에서 보이는 말풍선은 focus에서도 같게 보인다. 색이나 3D 이동만으로 선택을 알리지 않는다.
- Canvas 로딩 실패 시에도 DOM 책 목록, 이어 읽기, 책 추가, 상세 이동이 모두 동작해야 한다.
- dialog는 `aria-modal`, focus trap, 호출 요소로의 focus 복귀를 지킨다.

### 성능

- 방 geometry와 material은 재사용하고, 장식 mesh는 PC에서도 최소화한다.
- DPR은 현재처럼 상한을 두고, 저사양·모바일에서는 shadow 해상도와 장식물을 줄인다.
- 외부 환경맵, 대용량 texture, post-processing, 실시간 반사면은 사용하지 않는다.
- 첫 인터랙션보다 Canvas 로딩이 늦으면 DOM UI를 먼저 사용할 수 있어야 한다.

## 11. 구현 순서와 완료 기준

1. 현재 3D 책 선택·열기와 DOM fallback을 유지한 채 벽·바닥·책장·테이블의 큰 구도를 잡는다.
2. 책의 `pulling → turning → opening → zooming` 위치를 테이블 기준으로 조정한다.
3. 최근 이어 읽기 안내와 focus 말풍선을 DOM으로 장면 위에 배치한다.
4. 빈 상태, 로딩, 실패, reduced motion을 같은 방 구도 안에서 맞춘다.
5. 마지막에 edge, 고정된 미세한 기울기, 작은 소품으로 손그림 온도를 더한다.

완료 판단은 장식 수가 아니라 다음 질문으로 한다.

- [ ] 첫 화면이 카드 모음이 아니라 하나의 방으로 보이는가?
- [ ] 중앙 책장과 앞쪽 테이블의 앞뒤 관계가 즉시 읽히는가?
- [ ] 최근 책과 다음 시작 페이지를 3초 안에 찾을 수 있는가?
- [ ] 책을 고르면 선반에서 빠져나와 테이블에 놓이고 펼쳐지는 순서가 자연스러운가?
- [ ] 새 책이 저장 성공 뒤 테이블에서 빈 slot으로 들어가는가?
- [ ] 키보드와 Canvas 실패 상태에서도 모든 핵심 흐름을 완주할 수 있는가?
- [ ] reduced motion에서 기다림 없이 같은 결과에 도달하는가?
- [ ] Joguman의 구체적 캐릭터·소품을 복제하지 않고 공간감과 상호작용 원리만 가져왔는가?

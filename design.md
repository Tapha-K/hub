# 나의 책장 디자인 가이드

> 이 문서는 기존 독서방·타이머 서비스의 화면을 덧칠하는 지침이 아니다. `방`, `참여자`, `세션 종료`, `신뢰도`를 중심으로 한 기존 화면과 상태를 걷어내고, `책`, `마지막 책갈피`, `세션 기록 페이지`를 중심으로 화면 구조를 새로 만드는 기준이다.

## 1. 디자인의 역할

이 문서는 **나의 책장** PC 웹 MVP의 시각·상호작용 기준이다. 서비스의 핵심은 예쁜 서재를 수집하는 일이 아니라, 사용자가 마지막 책갈피에서 같은 책을 다시 열고 그날의 읽기를 기록하는 일이다.

```text
내 책장
→ 지난번 읽은 책을 연다
→ 끝난 페이지와 짧은 감상을 남긴다
→ 다음 시작 페이지를 확인한다
```

따라서 P0의 디자인은 이 흐름을 한눈에 읽게 해야 한다. 책장과 책등은 실제 서재를 닮은 탐색 장치이고, 꾸미기·리워드·친구 공개는 이 흐름이 완성된 뒤의 확장이다.

### 구현 전환 경계

- 기존 `rooms`, `sessions`, 타이머 기반 localStorage 상태와 그 화면 용어를 P0 책장 화면에 재사용하지 않는다.
- mock 단계에서도 `user`, `book`, `readingRecord`와 실제 API 응답에 가까운 상태만 사용한다. 그래야 서버 연동 때 화면을 다시 설계하지 않는다.
- 새 화면의 최소 단위는 `BookshelfScreen`, `BookSpine`, `ContinueReadingCard`, `BookDetailScreen`, `SessionRecordPage`, `AddBookDialog`, `SessionRecordDialog`이다.
- 이 문서의 서재 분위기는 CSS와 텍스트로 만든다. P0에 외부 표지 URL, 장식 사진, 이미지 생성 의존성을 추가하지 않는다.

## 2. 참고 이미지에서 가져올 것과 가져오지 않을 것

### 가져올 것

- 아이보리 종이, 밝은 나무, 짙은 잉크가 만드는 조용한 서재의 온도
- 책을 **세로로 꽂아** 여러 권 중 하나를 고르게 하는 밀도
- 책장 안에서 최근에 읽을 책을 먼저 보이게 하는 구조
- 책 한 권을 열었을 때 표지와 기록이 독립적인 한 권의 노트처럼 보이는 인상
- 실제 책장처럼 가로 선반이 내용을 구획하고, 여백이 숨 쉴 자리를 만드는 방식

### P0에서 가져오지 않을 것

- 외부 표지 이미지·도서 검색·별점·장르 평가·인용문 수집
- `TBR`, `DNF`, 목표 달성률, 읽기 퍼센트처럼 독서를 평가하거나 미완료를 강조하는 표기
- 모바일 앱의 하단 탭·풀스크린 카드·영상 통화 UI
- 책이 펼쳐지거나 선반을 드래그하는 고비용 3D/물리 애니메이션
- 식물·램프·액자 같은 장식 이미지를 화면마다 반복하는 방식

P0에서는 사용자 입력으로 만든 제목과 저자, 마지막 책갈피가 책을 식별한다. 외부 표지가 없어도 책마다 구별되는 **CSS 책등**을 사용한다.

## 3. 핵심 인상과 원칙

### 한 줄 인상

`내가 읽은 책과 생각이 차분히 쌓인, 다음 책갈피가 먼저 보이는 개인 서재`

### 원칙

1. **재개 행동이 장식보다 먼저 보인다.** 첫 화면의 가장 큰 행동은 `{n}쪽부터 이어 읽기`다.
2. **책은 물성이 있고 정보는 또렷하다.** 책등·선반으로 서재의 감각을 주되, 제목·페이지·날짜는 일반 텍스트로 선명하게 읽힌다.
3. **기록은 평가가 아니라 흔적이다.** 감상이 비어 있거나 며칠 쉬어도 빈 상태·오류·실패처럼 보이지 않는다.
4. **표면은 종이처럼, 조작은 웹답게.** 온기가 있는 배경과 얇은 경계를 쓰되, 버튼·입력·오류는 명확한 웹 UI 원칙을 따른다.
5. **한 번에 한 행동만 강조한다.** 같은 화면의 주 CTA는 하나다. 나머지는 조용한 텍스트 버튼이나 보조 버튼으로 둔다.

## 4. 디자인 토큰

구현 시 CSS custom property 또는 Tailwind token으로 아래 값을 먼저 고정한다. 브랜드 색을 책등마다 무작위로 과하게 쓰지 않는다.

| 역할 | 토큰 | 값 | 용도 |
| --- | --- | --- | --- |
| 앱 바탕 | `--canvas` | `#F6F1E8` | 전체 배경 |
| 종이 표면 | `--paper` | `#FFFDF8` | 본문·dialog·기록 페이지 |
| 선반 | `--wood` | `#B47B52` | 선반의 앞면, 강조선 |
| 선반 그림자 | `--wood-shadow` | `#7B4D32` | 선반 아래 얇은 음영 |
| 본문 잉크 | `--ink` | `#2D2925` | 제목·본문 |
| 보조 잉크 | `--muted` | `#746B63` | 날짜·보조 정보 |
| 경계 | `--line` | `#E3D9CB` | input·구분선 |
| 입력 경계 | `--input-border` | `#88786A` | input·textarea의 기본 테두리 |
| 행동 강조 | `--accent` | `#A55335` | 주 CTA |
| 행동 글자 | `--accent-foreground` | `#FFFDF8` | 주 CTA 안의 글자 |
| focus | `--focus` | `#2F6F8D` | 키보드 focus 외곽선 |
| 오류 | `--danger` | `#B54742` | 검증 오류 |

- 제목 폰트: 우선순위가 높은 책 제목과 페이지 제목에만 가독성 좋은 세리프 계열을 사용한다. 예: `"Noto Serif KR", Georgia, serif`.
- 본문·입력·수치: `"Pretendard", "Noto Sans KR", system-ui, sans-serif`.
- 기본 글자 크기는 16px 이상, 본문 행간은 1.55 이상으로 둔다.
- 모서리는 8px을 기본으로 하며, 모든 요소를 둥근 카드로 만들지 않는다. 종이·선반은 4–8px, dialog만 12px까지 허용한다.
- 그림자는 바닥감이 필요한 책등과 떠 있는 dialog에만 낮은 농도로 사용한다. glow나 그라디언트는 쓰지 않는다.
- 본문·입력·버튼 텍스트는 배경과 4.5:1 이상, 큰 제목은 3:1 이상의 대비를 확보한다. 옅은 나무색은 배경이나 구분선에만 쓰고 본문 글자에 쓰지 않는다.
- `--line`은 장식 구분선에만 쓴다. 입력의 유일한 테두리와 오류 상태에는 `--input-border`와 `--danger`처럼 3:1 이상 대비되는 색을 쓴다.
- keyboard focus는 `--accent`와 구분되는 2–3px 외곽선으로 보인다. hover가 없는 터치 환경에서도 현재 선택과 focus가 구별돼야 한다.

### 책등 팔레트와 정보 밀도

책등은 무작위 카드가 아니라 사용자가 다시 알아볼 수 있는 서재의 단위다. `book.id`를 기준으로 아래 팔레트와 폭을 결정해 새로고침 뒤에도 항상 같은 모습으로 둔다.

| 책등 톤 | 배경 | 제목 글자 | 폭 |
| --- | --- | --- | --- |
| 올리브 | `#556B4F` | `#FFFDF8` | 64px |
| 먹남색 | `#3D5368` | `#FFFDF8` | 76px |
| 벽돌 | `#9B5845` | `#FFFDF8` | 88px |
| 황토 | `#965F34` | `#FFFDF8` | 72px |
| 자주 | `#70505A` | `#FFFDF8` | 84px |
| 회청 | `#61747A` | `#FFFDF8` | 68px |

- 높이는 212–232px 범위에서 통일하고, 폭만 위 세 단계처럼 달리해 선반의 리듬을 만든다.
- 책등에는 `제목`과 작은 책갈피 탭의 `다음 시작 페이지` 또는 `첫 읽기`만 둔다. 마지막 기록 날짜는 재개 영역과 상세 화면으로 분리한다.
- 한국어 제목은 `writing-mode: vertical-rl`처럼 글자를 세로 흐름으로 읽을 수 있게 둘 수 있다. 글자를 이미지로 만들거나 한 글자씩 회전시키지 않으며, 전체 제목은 버튼의 접근 가능한 이름과 상세 화면에서 가로 텍스트로 제공한다.
- 선반은 `안쪽 배경 → 8–12px 앞면 → 얇은 아래 음영` 세 층으로만 깊이를 만든다. 장식 이미지·큰 그림자를 더하지 않는다.

### 타입과 간격

| 요소 | 크기 / 행간 | 성격 |
| --- | --- | --- |
| 페이지 제목 | 28 / 36px | serif, 600 |
| 책 상세 제목 | 24 / 34px | serif, 600 |
| 섹션 제목 | 18 / 28px | 600 |
| 본문·감상 | 16 / 25px | sans-serif |
| 날짜·상태·페이지 | 13–14 / 20px | sans-serif, `tabular-nums` |

- 기본 간격 단위는 8px이다. 섹션 사이 32px, 책등 사이 8px, 선반 위 책등 여백 16px을 기준으로 둔다.

## 5. PC 레이아웃

P0의 기준 폭은 1280px 이상이다. 콘텐츠 최대 너비는 1180px, 가로 여백은 32px으로 둔다. 화면은 독립된 카드들의 모음이 아니라 하나의 작업대처럼 느껴져야 한다.

```text
┌────────────────────────────────────────────────────────────┐
│ 나의 책장                                        닉네임      │
├────────────────────────────────────────────────────────────┤
│ 최근 이어 읽기 · 책 제목 · 지난번 58쪽                     │
│                                   [ 59쪽부터 이어 읽기 ]    │
├────────────────────────────────────────────────────────────┤
│ 읽는 중 · 4권                              [ 책 추가 ]       │
│ ────────────────────────────────────────────────────────  │
│   책등   책등   책등   책등                                 │
│ ═════════════════ 나무 선반 ════════════════════════════   │
└────────────────────────────────────────────────────────────┘
```

- 상단 바는 얇고 조용하게 둔다. 서비스 이름과 현재 사용자만 배치한다. P0에는 메뉴를 위한 빈 아이콘이나 별도 설정 화면을 만들지 않는다.
- `읽고 있는 책 추가`는 헤더의 보조 버튼 또는 `읽는 중` 섹션의 보조 행동이다. 재개 CTA와 같은 크기·색·위계로 놓지 않는다.
- `최근 이어 읽기`는 책장 위의 가장 먼저 읽히는 넓은 영역이다. 책 제목, 지난번 끝 페이지, 선택적 한 줄 감상, 주 CTA를 포함한다.
- `읽는 중` 선반은 최근 기록 순서의 책등을 보여 준다. 넓은 목록 카드보다 책장 비유를 살리되, 작은 화면에서도 제목을 읽을 수 있는 최소 너비를 지킨다.
- 책이 많아지면 처음부터 가로 스크롤을 강제하지 않는다. 2줄 이상의 선반 또는 `더 보기`로 확장한다.
- P0의 빈 책장은 선반과 한 권이 들어갈 자리를 남기고 `첫 책이 들어올 자리예요.`와 `읽고 있는 책 추가` CTA를 보여 준다.

### 폭별 배치 규칙

| 화면 폭 | 레이아웃 | 반드시 유지할 것 |
| --- | --- | --- |
| 1280px 이상 | 최대 1180px 단일 작업대, 재개 카드와 책 추가를 같은 줄에 배치 가능 | 재개 CTA가 접히지 않음 |
| 1024–1279px | 최대 폭 없이 32px 여백, 재개 카드 내부 정보를 두 줄로 허용 | 책등 최소 폭 64px |
| 768–1023px | 24px 여백, 재개 카드와 책 추가를 세로 배치 | 버튼의 전체 문구와 페이지 수 |
| 767px 이하 | 16px 여백, 선반만 가로 스크롤 가능 | 헤더·dialog·기록 입력은 가로 스크롤 금지 |

### 좁은 화면

P0 검증은 PC 우선이지만 768px 아래에서 내용이 사라지면 안 된다.

- 바깥 여백을 16px로 줄이고, 최근 이어 읽기와 책 추가를 세로로 쌓는다.
- 책등은 최소 64px 폭을 지키며 필요하면 가로 스크롤한다.
- 모바일 전용 하단 탭, 고정 재생바, 새로운 정보 구조는 만들지 않는다.

## 6. 화면별 기준

### 6.1 내 책장

**목적:** 돌아온 사용자가 다음 읽기를 고르는 화면.

- 헤더 아래 첫 영역에 `최근 이어 읽기`를 둔다. 기록이 있는 `READING` 책이 없다면, 가장 최근에 추가한 새 책을 `처음 읽기` 행동으로 승격한다. 등록한 책도 없다면 이 영역을 빈 책장 CTA로 바꾼다.
- 재개 카드에는 다음 네 정보만 우선한다.
  1. 책 제목과 선택적 저자
  2. `지난번 58쪽까지 읽었어요`
  3. 가장 최근 감상 한 줄 — 있을 때만
  4. `{nextStartPage}쪽부터 이어 읽기` 주 CTA
- `읽는 중`은 명확한 섹션 제목과 책 수를 보인다. `TBR`, `DNF` 같은 분류는 P0에 만들지 않는다.
- 책등에는 제목과 다음 시작 페이지 또는 `처음 읽기`만 표시한다. 같은 제목이 겹쳐도 P0에서는 중복 등록을 허용하므로 저자 또는 등록 순서로 구별한다.
- 제목과 저자가 모두 같은 책은 상세 상단에 `책장에 꽂은 날짜`를 보조 정보로 둔다. 책등 링크의 접근 가능한 이름에는 제목·저자·등록일·다음 시작 페이지를 모두 넣어 구별한다.
- 검색·정렬·필터는 P0에 넣지 않는다. 책이 많아져도 `최근 기록 순서`라는 한 가지 규칙을 먼저 검증한다.

### 6.2 CSS 책등

외부 표지 대신 책등은 작은 세로형 컴포넌트로 만든다.

- 기본 크기: 폭 64–88px, 높이 212–232px. 제목이 긴 경우 좁은 책등에서 억지로 여러 줄을 만들지 않고, 전체 제목은 상세와 접근 가능한 이름으로 확인한다.
- 배경은 앞서 정의한 팔레트에서 `book.id`로 결정한다. 같은 책은 새로고침 뒤에도 같은 색과 폭을 유지한다.
- 책등의 좌측에 3–4px 짙은 띠와 하단의 미세한 그림자를 넣어 두께감을 만든다.
- 선택·hover 상태는 살짝 위로 이동(`translateY(-4px)`)하고 focus ring을 보여 준다. 색만으로 상태를 나타내지 않는다.
- 한국어 제목은 한 글자씩 임의로 회전하지 않는다. 필요할 때만 자연스러운 세로 흐름을 쓰고, 상세 화면에서는 항상 가로 텍스트로 전체 제목을 확인하게 한다.
- 책등은 클릭 가능한 `div`가 아니라 `/books/:bookId`로 가는 `<a>`로 구현한다. 마우스 hover가 없어도 keyboard focus와 Enter 동작이 가능해야 한다.
- 실제 표지처럼 보이게 이미지나 가짜 ISBN, 별점, 진행률을 넣지 않는다.

### 6.3 책 상세 — 펼쳐진 독서 노트

**목적:** 한 권의 현재 위치를 확인하고 기록을 남기는 화면.

```text
← 책장으로
┌──────────────────────────────────────────────────────────┐
│ [책등]  제목 · 저자                                       │
│         지난번 58쪽까지 읽었어요                          │
│         [ 59쪽부터 이어 읽기 ]                            │
├──────────────────────────────────────────────────────────┤
│ 독서 노트                                                  │
│  4월 10일 · 42–58쪽                                        │
│  오늘은 문장이 천천히 읽혔다.                              │
│  ──────────────────────────────────────────────────────  │
│  … 오래된 기록부터                                         │
└──────────────────────────────────────────────────────────┘
```

- 상단은 책등, 제목·저자, `BookmarkSummary`, 주 CTA를 한 묶음으로 둔다.
- `지난번 {endPage}쪽까지 읽었어요.`와 `{nextStartPage}쪽부터 이어 읽기`는 세션 기록 목록보다 위에 둔다.
- 사용자가 `이어 읽기`를 누르면 상단 CTA 아래에 `59쪽부터 읽어요. 다 읽고 돌아오면 기록을 남겨 주세요.`라는 짧은 읽기 준비 안내와 `이번 읽기 기록 남기기` 행동을 표시한다. 이는 타이머·감시·진행률이 아니라 다음 행동을 알려 주는 일시 상태다.
- 기록 목록은 오래된 순서로, 한 장의 종이가 이어지는 듯 얇은 선으로 나눈다. 각 기록에서 페이지 범위와 날짜가 감상보다 먼저 읽혀야 한다.
- 감상이 없을 때는 빈 카드가 아니라 `날짜 · 페이지 범위`만 가진 낮은 밀도의 행으로 둔다.
- 기록이 4개 이상이면 상단에 `가장 최근 기록으로` 앵커를 둔다. 저장 성공 뒤에는 새 기록 행으로 스크롤하고 그 행에 포커스를 옮겨 변화가 보이게 한다.
- 기록 날짜는 사람이 읽는 형식으로 보이되 HTML `time` 요소와 원본 ISO 날짜를 함께 둔다. 페이지 범위는 `42–58쪽`처럼 한 덩어리로 읽히게 한다.
- P0의 마지막 서평은 동작하지 않는 과한 잠금 연출 대신 `완독하면 이 책의 마지막 장에 긴 서평을 남길 수 있어요.`라는 작은 미리보기로 끝낸다.

### 6.4 책 등록 dialog

**목적:** 첫 책을 30초 안에 내 책장에 넣는다.

- 제목을 첫 필드이자 가장 넓은 입력으로 둔다.
- 저자와 시작 페이지는 보조 입력이다. 시작 페이지는 `1`을 기본값으로 하며, 이미 읽던 책을 등록할 때만 바꾼다.
- 주 CTA는 `책장에 꽂기`, 보조 행동은 `취소`다.
- 필드 오류는 입력 바로 아래에 문장으로 보여 준다. `제목을 입력해 주세요.`처럼 원인을 말한다.
- 저장 중에는 dialog를 닫거나 중복 저장할 수 없고, 실패하면 입력한 값은 그대로 남는다.

### 6.5 세션 기록 dialog

**목적:** 읽은 뒤 계산 없이 끝난 페이지와 생각 한 줄을 남긴다.

- 상단에 자동 시작 위치를 정보로 보여 준다. 예: `59쪽부터 읽기로 했어요.`
- 사용자가 직접 쓰는 첫 필드는 `오늘 끝낸 페이지`다. 끝난 페이지 입력 뒤 `59–74쪽을 읽었어요.`를 즉시 미리 보여 준다.
- `시작 위치 직접 바꾸기`는 다시 읽기·건너뛰기 예외에서만 접힌 보조 행동으로 둔다.
- 감상 textarea는 선택이며, `한 줄이면 충분해요. 비워 두어도 괜찮아요.`를 안내한다.
- 주 CTA는 `기록 페이지 추가`다. 성공하면 새 기록과 갱신된 다음 시작 페이지를 보여 주고 dialog를 닫는다.

### 6.6 의미 구조·키보드·화면 전환

- 책장 화면의 DOM 순서는 `h1(나의 책장) → h2(최근 이어 읽기) → h2(읽는 중) → ul/li(책등)`를 따른다. 시각적 선반은 이 의미 구조를 가리지 않는다.
- `ContinueReadingCard` 전체와 내부 CTA를 동시에 클릭 가능하게 만들지 않는다. 재개 CTA 하나만 `59쪽부터 이어 읽기`의 행동을 가지며, 책등은 `/books/:bookId`로 가는 별도 링크다.
- 책등 링크와 재개 CTA는 책 상세 URL로 이동한다. 재개 CTA만 `isReadingContextActive`를 시작하고, 일반 책등 클릭은 읽기 준비 상태를 시작하지 않는다.
- 화면 전환 뒤에는 새 화면의 `h1`으로 포커스를 옮긴다. 책장으로 돌아갈 때는 이전에 선택한 책등 또는 재개 CTA로 포커스를 되돌린다.
- dialog는 `role="dialog"`, `aria-modal="true"`로 제목과 설명을 연결하고 포커스를 안에 가둔다. 닫히면 호출한 요소로 포커스를 돌려준다. 제목·감상 등 작성한 내용이 있을 때 취소·닫기를 누르면 `작성 중인 내용이 사라져요.` 확인을 한 번 더 보여 준다.
- 모든 입력 폼은 native `<form>`으로 Enter 제출을 지원한다. 필수·선택 여부를 글자로 표시하고, 오류 필드는 `aria-invalid`와 설명·오류 문장을 연결한다.
- 끝난 페이지 입력으로 계산된 범위와 저장 성공은 `aria-live="polite"` 또는 `role="status"`로 알린다. 오류는 toast만으로 전달하지 않고 첫 오류 필드로 포커스를 옮긴다.

## 7. 상태와 피드백

| 상태 | 보여 줄 것 | 피할 것 |
| --- | --- | --- |
| 첫 방문 | 빈 선반, 첫 책 CTA | 비어 있음·부족함 경고 |
| 기록 없음 | `n쪽부터 처음 읽기` | 0% 진행률 |
| 저장 중 | CTA 비활성화, `저장 중…` | 화면이 이미 성공한 것처럼 바뀜 |
| 저장 성공 | 새 기록과 새 시작 페이지 | 토스트만 남기고 핵심 정보 미갱신 |
| 저장 실패 | 입력 유지, 재시도 CTA, 짧은 오류 문장 | draft 초기화 |
| 며칠 쉬었음 | 마지막 책갈피와 재개 CTA | 스트릭 실패·결석 표현 |

- 성공 toast는 보조 수단일 뿐이다. 저장 후 바뀐 기록 페이지와 `nextStartPage`가 실제 성공 피드백이다.
- 버튼은 loading, disabled, focus, error 상태를 모두 가진다.
- 아이콘만 쓴 버튼에는 항상 `aria-label`을 둔다. 색 대비와 키보드 focus를 기본 품질로 검증한다.
- 서버에서 책장 또는 책 상세를 처음 불러오는 동안에는 재개 스트립 하나와 4–5개의 책등 형태 skeleton을 보여 줘 레이아웃이 흔들리지 않게 한다. 실제 기록처럼 보이는 샘플 문장이나 가짜 페이지를 loading 상태에 넣지 않는다.
- 책 상세를 찾을 수 없거나 삭제된 데이터를 열었을 때는 `책장으로 돌아가기` 하나를 제공한다. 브라우저의 빈 화면이나 기술적 오류 문구로 끝내지 않는다.
- 저장 응답을 확인할 수 없으면 즉시 같은 기록을 재전송하지 않는다. draft를 유지한 채 책 상세를 먼저 재조회하고, 기록이 없을 때만 사용자가 다시 저장을 선택하게 한다.
- 400% 확대 또는 320 CSS px 폭에서도 본문·dialog·주 CTA가 잘리지 않아야 한다. 가로 스크롤은 책등 목록에서만 허용하고, 다음 책 일부가 보여 스크롤 가능성을 알린다.
- 모든 조작 대상은 최소 24 × 24px, 주 CTA와 dialog의 확인·취소 버튼은 최소 44 × 44px의 실제 클릭 영역을 가진다.

## 8. 모션

- 책등 hover: 160–200ms, `translateY(-4px)` 이내.
- dialog: 180ms 정도의 opacity/scale 전환만 사용한다.
- 기록 저장 성공: 새 기록 행이 짧게 나타나는 정도만 허용한다.
- 책이 선반에서 나와 펼쳐지는 장면, 쪽 넘김, 리워드 폭죽, 장시간 타이머 애니메이션은 P0에 넣지 않는다.
- `prefers-reduced-motion`에서는 이동·scale 모션을 끈다.
- `prefers-reduced-motion`에서는 저장 뒤 자동 스크롤과 부드러운 화면 전환도 끈다.

## 9. 구현 전달 체크포인트

디자인을 구현할 때 아래 순서를 지킨다. 이 순서는 화면이 기존 서비스의 모양만 바뀌고 동작은 남는 일을 막는다.

1. 기존 방·타이머 화면과 상태를 새 P0 도메인 모델로 교체한다.
2. API 없이도 `책 등록 → 책 상세 → 끝난 페이지 기록 → 다음 시작 페이지`가 mock 데이터에서 한 번 완주되는지 확인한다.
3. 로딩·실패·빈 상태를 주 화면과 dialog에 넣는다.
4. 그 뒤 색·선반·책등의 밀도와 미세 모션을 적용한다.

구현 완료 판단은 "참고 이미지와 닮았는가"가 아니라, 새로고침 뒤에도 사용자가 다음에 읽을 책과 시작 페이지를 바로 찾는가로 한다.

## 10. P1 이후 확장 원칙

- **완독:** `COMPLETED` 선반과 마지막 서평 페이지를 추가한다. 완료 배지는 점수나 순위가 아니라 한 권의 기록이 닫혔음을 알린다.
- **보관함:** `ARCHIVED` 책은 실패나 삭제가 아니다. `잠시 보관하기`는 보조 행동으로 두고, 다시 꺼낼 때 마지막 책갈피를 강조한다.
- **기록 정정:** 수정·삭제는 가장 최근 기록에만 작게 제공한다. 이전 기록에 편집 아이콘을 반복 배치하지 않는다.
- **읽기 기록 달력:** 기록을 남긴 날의 수만 조용히 보여 준다. 빈 날은 실패가 아니며, 페이지 수·읽은 시간으로 색 농도를 매기지 않는다.
- **생각 키워드:** P2에서 감상 아래 작은 칩으로 제공한다. 모아보기에서도 책 제목·페이지 범위·날짜를 함께 보여 문맥을 잃지 않게 한다.
- **커스터마이징·친구 서재:** 개인 재개 흐름이 검증된 뒤 추가한다. 꾸미기는 선택 보상이고, 친구에게는 사용자가 선택한 완독 책과 서평만 공개한다.

## 11. 구현 전 점검

- [ ] 첫 화면에서 최근 이어 읽기 CTA가 책 추가보다 먼저 보이는가?
- [ ] 외부 표지 없이도 책 제목·다음 시작 페이지로 책을 구별할 수 있는가?
- [ ] 끝난 페이지를 입력할 때 사용자가 읽은 페이지 수를 계산하지 않아도 되는가?
- [ ] 저장 성공·실패 뒤 서버 응답과 화면 상태가 일치하는가?
- [ ] 빈 책장, 감상 없음, 며칠의 공백이 실패처럼 보이지 않는가?
- [ ] 키보드만으로 책 열기, dialog 입력·저장·닫기가 가능한가?
- [ ] `이어 읽기` CTA와 일반 책등 클릭이 각각 읽기 준비 시작·책 상세 열기라는 다른 결과를 명확히 주는가?
- [ ] 불확실한 저장 응답 뒤 중복 기록을 만들지 않고 먼저 재조회하는가?
- [ ] 책등 색상과 순서가 새로고침 뒤에도 같은 책을 같은 방식으로 식별하게 하는가?
- [ ] 기존 독서방·타이머 용어와 데이터 구조가 새 화면에 남아 있지 않은가?

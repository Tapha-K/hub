# 나만의 AI 협업 워크플로우

> 초안 · 2026-07-24
> 저장소의 문서, 커밋, PR 흐름과 `AGENTS.md`에 남은 규칙을 기준으로 정리했다.

## 1. 프로젝트와 목표

잇장은 읽던 책의 마지막 책갈피와 짧은 생각을 저장해 다음 독서를 쉽게 시작하게 돕는 서비스다. React 화면, Spring Boot API, MySQL을 한 번에 연결하는 수직 슬라이스를 먼저 만들고, 이후 완독·보관·타이머·도서 검색을 붙였다.

AI와 일할 때의 목표도 같다. 많은 코드를 한꺼번에 만드는 것이 아니라, **사용자가 확인할 수 있는 한 흐름을 정하고 화면부터 DB까지 완주하는 것**이다.

## 2. 3주 동안 바뀐 방식

| 기간 | 한 일 | AI와 일한 방식 |
| --- | --- | --- |
| 1주차 · 7/6–7/10 | React 환경, 기획 문서, HTML 프로토타입, shadcn/ui 기반 화면 | 먼저 화면과 클릭 흐름을 문서로 만들고 빠르게 눈으로 확인했다. |
| 2주차 · 7/13–7/16 | 독서방 중심 콘셉트를 책장·책갈피 중심으로 재정의, mock 수직 슬라이스, Spring Boot/MySQL/API 연동 | 개발자·QA·첫 방문·재방문 사용자 역할의 Agent 의견을 비교하고, 합의를 PRD와 API 계약으로 고정한 뒤 구현했다. |
| 3주차 · 7/20–7/24 | 완독·보관, 기록 정정, 테스트, 타이머, Google Books, 아키텍처 문서 | 작은 기능 단위로 구현→검증→PR을 반복하고, 실제 코드의 데이터 흐름과 다음 기술 부채를 문서화했다. |
| 후속 · 7/26–7/27 | Google 로그인·서버 세션, 글귀 재노출 루프, 활동 달력, 컴포넌트 분리, 페이지 통합형 3D와 로컬 QA 정리 | gstack 검토로 인증·성능·상호작용 경계를 확정하고, 작은 PR 단위로 구현·회귀 테스트·문서화를 반복했다. |

가장 큰 변화는 **AI에게 바로 구현을 시키는 방식에서, 먼저 기준 문서를 만들고 그 기준으로 구현과 검증을 반복하는 방식으로 바뀐 것**이다.

## 3. 기본 작업 순서

1. **한 문장으로 결과를 정한다.**
   예: `세션 기록을 저장하면 다음 시작 페이지가 갱신되고 새로고침 뒤에도 유지된다.`

2. **Agent가 기존 맥락을 먼저 읽는다.**
   `AGENTS.md`, README, 관련 문서, 호출 경로, 테스트, GitHub Issue/Wiki를 확인한다. 이미 있는 패턴을 재사용한다.

3. **모호한 제품 문제만 역할 Agent로 검토한다.**
   프론트엔드 구현, QA·범위, 첫 방문 사용자, 재방문 사용자 관점으로 나눠 충돌을 찾는다. 단순 구현에는 여러 Agent를 쓰지 않는다.

4. **결정을 문서와 계약으로 고정한다.**
   PRD → 사용자 흐름 → 클릭 명세 → 컴포넌트 책임 → API 계약 순으로 필요한 문서만 갱신한다.

5. **가장 작은 수직 슬라이스를 구현한다.**
   화면 상태 → API → 서비스 검증 → DB 저장 → 화면 재조회가 한 번에 동작하게 만든다.

6. **검증하고 결과를 남긴다.**
   가장 작은 회귀 테스트와 빌드를 실행하고, 직접 사용자 흐름을 시연한다. PR에는 변경, 데이터 영향, 검증, 한계, 다음 작업을 기록한다.

```text
한 문장 결과
→ 기존 코드·문서 읽기
→ 필요한 관점만 토론
→ 문서·API 계약 확정
→ 화면–서버–DB 수직 구현
→ 테스트·시연
→ PR과 다음 Issue
```

## 4. 사용하는 Skill과 Agent

### Agent

| 역할 | 맡기는 일 | 맡기지 않는 일 |
| --- | --- | --- |
| 메인 Codex Agent | 저장소 탐색, 구현, 테스트, 문서, PR 준비 | 제품 우선순위의 최종 결정 |
| 개발자 관점 Agent | 상태 모델, 컴포넌트 경계, 구현 가능성 검토 | 사용자 가치 단독 판단 |
| QA·범위 Agent | 예외, 복구 경로, MVP 범위 점검 | 요구사항 확장 |
| 사용자 관점 Agent | 첫 방문·재방문 흐름의 마찰 설명 | 기술 구조 결정 |

역할 Agent의 결과는 토론 기록일 뿐이다. 최종 기준은 README와 제품 문서에 다시 적은 내용이다.

### Skill과 도구

| Skill·도구 | 쓰는 순간 | 원칙 |
| --- | --- | --- |
| `ui-skills` | UI를 새로 설계하거나 크게 바꿀 때 | 일회성 디자인 참고로 사용하고 런타임 의존성으로 넣지 않는다. |
| `shadcn/ui` | React의 버튼·다이얼로그·입력 같은 기본 UI | 필요한 컴포넌트만 생성해 프로젝트 안에서 수정한다. |
| `ponytail` | 구현·리팩터링 범위를 줄일 때 | 기존 코드, 표준 기능, 설치된 의존성을 먼저 쓰고 최소 diff를 만든다. 이번 정리부터 명시적으로 적용한다. |
| Mermaid | 화면–서버–DB 흐름을 공유할 때 | README에서 바로 렌더링되는 텍스트 다이어그램으로 유지한다. |
| Vitest·JUnit | 비즈니스 분기나 회귀 위험이 있을 때 | 깨지면 핵심 동작을 바로 알 수 있는 가장 작은 테스트를 남긴다. |

## 5. 잘 작동한 프롬프트 형태

구현 전에 결과와 검증 기준을 함께 준다.

```text
관련 코드와 문서를 먼저 읽어줘.
사용자가 책을 등록하고 세션 기록을 저장한 뒤,
새로고침해도 다음 시작 페이지가 유지되게 해줘.
기존 패턴을 재사용하고, 가장 작은 테스트와 빌드로 확인해줘.
```

검토할 때는 수정 권한을 구분한다.

```text
첫 방문 사용자와 재방문 사용자 관점에서 이 흐름의 마찰을 찾아줘.
아직 코드는 바꾸지 말고, 심각도와 가장 작은 해결책만 정리해줘.
```

## 6. 내가 지키는 경계

- AI가 낸 아이디어를 바로 요구사항으로 만들지 않는다.
- 구현 전에 현재 코드와 모든 호출 경로를 읽게 한다.
- 제품 기준은 대화가 아니라 저장소 문서에 남긴다.
- 화면 성공 상태는 mock이 아니라 서버 응답을 기준으로 한다.
- 보안, 데이터 손실 방지, 접근성, 오류 복구는 최소화 대상에서 제외한다.
- 단순한 작업에는 역할 Agent를 늘리지 않는다.
- 완료는 “코드 생성”이 아니라 테스트와 실제 흐름 확인까지다.

## 7. 10분 발표 진행안

| 누적 | 내용 | 말할 핵심 |
| --- | --- | --- |
| 0:30 | 제목 | AI가 대신 개발한 이야기가 아니라 함께 결정하는 방식을 소개한다. |
| 1:15 | 서비스 소개 | 잇장은 마지막 책갈피에서 다시 시작하게 돕는다. |
| 2:05 | 3주 변화 | 화면 확인 → 수직 슬라이스 → 작은 반복으로 발전했다. |
| 3:15 | 작업 순서 | 읽기, 명세, 구현, 검증, 기록의 한 바퀴를 설명한다. |
| 4:10 | Skill·Agent | 필요한 역할만 쓰고 최종 기준은 문서에 둔다. |
| 5:00 | 전환 사례 | 독서방·신뢰도 중심에서 책장·재개 중심으로 바꾼 과정을 설명한다. |
| 5:50 | 구조 | React → Spring Boot → MySQL, Google Books 흐름을 설명한다. |
| 7:50 | 시연 | 책 검색·등록 → 이어 읽기 → 기록 → 새로고침 복구를 보여 준다. |
| 8:30 | 배운 점 | AI의 속도보다 기준과 검증 루프가 중요했다. |
| 10:00 | 질의응답 | 아래 예상 질문으로 대화를 연다. |

### 시연 체크리스트

실행 환경 준비, 단계별 기대 결과와 실패 대응은 [로컬 실행과 통합 QA](docs/local-qa-and-demo.md)를 기준으로 합니다.

1. 글귀 배너에서 책을 다시 펼친다.
2. 마지막 책갈피 다음 페이지를 확인한다.
3. 끝난 페이지, 선택적 감상과 글귀를 저장한다.
4. 책장과 읽기 기록 달력의 갱신을 확인한다.
5. 새로고침 후 같은 값이 복구되는 것을 확인한다.

### 예상 질문

**왜 여러 Agent를 항상 쓰지 않나요?**
관점 충돌이 필요한 제품 문제에만 효과가 있다. 단순 구현에서는 맥락 전달과 합의 비용이 더 크다.

**AI 코드의 품질은 어떻게 확인하나요?**
문서와 API 계약을 기준으로 diff를 검토하고, 테스트·빌드·실제 사용자 흐름을 모두 확인한다.

**가장 큰 실패 또는 수정은 무엇이었나요?**
초기 독서방·타이머 중심 콘셉트가 “다시 읽기” 문제보다 기능을 앞세웠다. 역할 Agent 검토 뒤 책장·책갈피 중심으로 과감히 재설계했다.
